'''lanche = ('Hamburguer', 'Suco', 'Pizza', 'Pudim')

for c in lanche:
    print(f'Vou comer {c}!')

for c in range(len(lanche)):
    print(f'Vou comer {lanche[c]}!')

for pos, c in enumerate(lanche):
    print(f'Vou comer {c}, na posição {pos}.')'''

#Para saber o indice

num = (3, 2, 6, 8, 3)

print(num.index(3, 1)) # O numero apos a virgula, significa: Que posição tem '3' começando do indice '1'.